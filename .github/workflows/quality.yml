name: Quality

on:
  pull_request:

permissions:
  contents: write

jobs:
  format-and-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/dm857-modules-10-12

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Ruff
        run: python -m pip install "ruff>=0.5,<1"

      - name: Format repository
        run: ruff format .

      - name: Restore permanent workflow and commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout origin/main -- .github/workflows/quality.yml
          git add .
          git commit -m "format DM857 modules 10 to 12"
          git push origin HEAD:feat/dm857-modules-10-12
