# DM857 compact module layout

The course page uses the global application header for course identity and a single compact module context bar for the active module. Duplicate course cards, module selectors and repeated summaries are intentionally avoided when only one module is available.
